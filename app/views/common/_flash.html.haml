.container-fluid.container-flash
  .row
    .content-flash
      - flash.each do |k,v|
        %div{ class: "alert alert-message alert-#{k} alert-dismissible alert-flash fade in" }
          %button{ class: 'close', data: { dismiss: 'alert' } }
            .fa.fa-close
          = v